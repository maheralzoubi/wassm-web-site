services:
  - type: web
    name: my-vite-drizzle-app
    env: node
    buildCommand: "npm run build"
    startCommand: "npm run start"
    plan: free
    envVars:
      - key: DATABASE_URL
        value: your-database-url
